(()=>{
  if(window.__DOGSON_SYSTEM_STATUS_V1720__) return;
  window.__DOGSON_SYSTEM_STATUS_V1720__=true;
  window.DOGSON_APP_VERSION='1.7.2';
  const $=(s,r=document)=>r.querySelector(s);
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const ymd=v=>{const m=String(v||'').match(/(20\d{2})-(\d{2})-(\d{2})/);return m?`${m[1]}-${m[2]}-${m[3]}`:''};
  const json=async name=>{try{const r=await fetch(`./data/${name}.json?truth=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(String(r.status));return await r.json()}catch(e){return {_error:e?.message||'讀取失敗'}}};
  const explicit=(o,keys=[])=>{for(const k of keys){const d=ymd(o?.[k]);if(d)return d}return''};
  const rowDate=o=>{const count={};for(const r of (Array.isArray(o?.rows)?o.rows:[])){const d=ymd(r?.quote_date)||ymd(r?.date)||ymd(r?.trade_date);if(d)count[d]=(count[d]||0)+1}return Object.entries(count).sort((a,b)=>b[1]-a[1])[0]?.[0]||''};
  const intradayDate=o=>ymd(o?.bridge?.trade_date)||rowDate(o)||explicit(o,['trade_date']);
  const daytradeDate=o=>explicit(o,['source_trade_date','trade_date'])||rowDate(o);
  const chipDate=o=>{const ds=[];if(Array.isArray(o?.rows)){for(const r of o.rows){const d=ymd(r?.chip_date)||ymd(r?.foreign_date)||ymd(r?.date);if(d)ds.push(d)}}else if(o&&typeof o==='object'){for(const v of Object.values(o)){if(!Array.isArray(v))continue;for(const r of v){const d=ymd(r?.chip_date)||ymd(r?.foreign_date)||ymd(r?.date);if(d)ds.push(d)}}}return ds.sort().at(-1)||explicit(o,['trade_date','date'])};
  function taipeiClock(){try{const p={};new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Taipei',year:'numeric',month:'2-digit',day:'2-digit',weekday:'short',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date()).forEach(x=>{if(x.type!=='literal')p[x.type]=x.value});const minute=Number(p.hour)*60+Number(p.minute);return{today:`${p.year}-${p.month}-${p.day}`,session:['Mon','Tue','Wed','Thu','Fri'].includes(p.weekday)&&minute>=535&&minute<=815}}catch{return{today:'',session:false}}}
  function currentMode(){try{return mode||'intraday'}catch{return'intraday'}}
  function portfolioView(){try{return !!portfolioOnly}catch{return document.documentElement.classList.contains('dogson-portfolio-view')}}
  function mission(m,portfolio){if(portfolio)return{icon:'💼',title:'我的庫存',text:'先看哪些持股需要處理，再看市場背景；不是拿來找今天最強的新股票。'};if(m==='close')return{icon:'🌙',title:'盤後作戰',text:'收盤後找值得明天繼續蹲的波段候選，再確認進場位置與風險。'};if(m==='daytrade')return{icon:'🎯',title:'當沖執行',text:'只有合格盤中即時狀態才顯示可執行候選；其他時間不把歷史訊號冒充現在機會。'};return{icon:'🔎',title:'找波段',text:'盤中即時層通過品質門檻才用5分鐘雷達；其他時間自動使用最近完整盤後資料。'}}
  function installStyle(){if($('#dogsonSystemStatusStyle'))return;const s=document.createElement('style');s.id='dogsonSystemStatusStyle';s.textContent=`.dogson-mission-v1700{margin:9px 0 10px;padding:11px 12px;border:1px solid #dfe5e1;border-radius:14px;background:#fff;box-shadow:0 3px 14px rgba(34,51,43,.04)}.dogson-mission-title-v1700{font-size:14px;font-weight:950;color:#25312c}.dogson-mission-text-v1700{font-size:10.5px;line-height:1.55;color:#75817b;margin-top:3px}.dogson-truth-v1700{margin:8px 0 10px;border:1px solid #dfe5e1;border-radius:14px;background:#fff;overflow:hidden}.dogson-truth-v1700>summary{list-style:none;padding:10px 12px;display:flex;gap:8px;align-items:center;justify-content:space-between;cursor:pointer}.dogson-truth-v1700>summary::-webkit-details-marker{display:none}.dogson-truth-title-v1700{font-size:11px;font-weight:900;color:#304038}.dogson-truth-sub-v1700{font-size:9.5px;color:#7e8984;margin-top:2px}.dogson-truth-badge-v1700{font-size:9px;font-weight:900;padding:5px 7px;border-radius:999px;white-space:nowrap}.dogson-truth-badge-v1700.ok{background:#edf6f0;color:#337256}.dogson-truth-badge-v1700.warn{background:#faf3df;color:#8b681b}.dogson-truth-badge-v1700.bad{background:#faecee;color:#b9444e}.dogson-truth-body-v1700{padding:0 10px 10px}.dogson-truth-grid-v1700{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px}.dogson-truth-item-v1700{padding:8px;border-radius:10px;background:#f7f9f7;border:1px solid #e8ece9}.dogson-truth-k-v1700{font-size:9px;color:#7d8983}.dogson-truth-v-v1700{font-size:11px;font-weight:900;color:#2b3731;margin-top:2px}.dogson-truth-note-v1700,.dogson-truth-source-v1700{font-size:9px;line-height:1.5;color:#7c8882;margin-top:8px}.dogson-truth-source-v1700{padding-top:8px;border-top:1px dashed #e0e6e2}html[data-dogson-theme="dark"] .dogson-mission-v1700,html[data-dogson-theme="dark"] .dogson-truth-v1700{background:#202522;border-color:#343b37}html[data-dogson-theme="dark"] .dogson-mission-title-v1700,html[data-dogson-theme="dark"] .dogson-truth-title-v1700,html[data-dogson-theme="dark"] .dogson-truth-v-v1700{color:#eef2ef}html[data-dogson-theme="dark"] .dogson-truth-item-v1700{background:#252b27;border-color:#343b37}@media(max-width:560px){.dogson-truth-grid-v1700{grid-template-columns:repeat(2,minmax(0,1fr))}}`;document.head.appendChild(s)}
  function setVersion(){const sub=$('header .sub');if(sub)sub.textContent='犬子老師・決策雷達 v1.7.2';document.documentElement.dataset.dogsonAppVersion='1.7.2'}
  function renderMission(){const nav=$('#dogsonViewNav');if(!nav)return;let box=$('#dogsonMissionV1700');if(!box){box=document.createElement('section');box.id='dogsonMissionV1700';box.className='dogson-mission-v1700';nav.after(box)}const x=mission(currentMode(),portfolioView());const html=`<div class="dogson-mission-title-v1700">${x.icon} ${esc(x.title)}</div><div class="dogson-mission-text-v1700">${esc(x.text)}</div>`;if(box.dataset.h!==html){box.innerHTML=html;box.dataset.h=html}}
  let snapshot=null;
  async function loadTruth(){
    const [market,intra,close,hourly,daytrade,chips,status,system]=await Promise.all(['market','intraday','close','hourly','daytrade','chip_history','status','system_status'].map(json));
    const fallback={market:explicit(market,['trade_date']),intraday:intradayDate(intra),close:explicit(close,['trade_date']),hourly:explicit(hourly,['trade_date']),daytrade:daytradeDate(daytrade),chips:chipDate(chips)};
    const dates=system?.dates&&typeof system.dates==='object'?{market:ymd(system.dates.market)||fallback.market,intraday:ymd(system.dates.intraday)||fallback.intraday,close:ymd(system.dates.close)||fallback.close,hourly:ymd(system.dates.hourly)||fallback.hourly,daytrade:ymd(system.dates.daytrade)||fallback.daytrade,chips:ymd(system.dates.chips)||fallback.chips}:fallback;
    const valid=Object.values(dates).filter(Boolean).sort();const latest=ymd(system?.latest_completed_trade_date)||[dates.market,dates.close].filter(Boolean).sort().at(-1)||valid.at(-1)||'';const errors=[market,intra,close,hourly,daytrade,chips,status].filter(x=>x?._error).length;
    const intradayStale=system?.freshness?.intraday_stale===true||!!(latest&&(!dates.intraday||dates.intraday<latest));const daytradeStale=system?.freshness?.daytrade_stale===true||!!(latest&&(!dates.daytrade||dates.daytrade<latest||(dates.intraday&&dates.daytrade!==dates.intraday)));
    const q=system?.operational?.intraday_quality||{quote_rows:Number(intra?.quote_layer?.coverage||0),row_count:Number(intra?.quote_layer?.row_count||(intra?.rows||[]).length),quote_coverage_pct:0,coverage_min_pct:80,latest_quote_time:intra?.quote_layer?.latest_time||null};if(!q.quote_coverage_pct&&q.row_count)q.quote_coverage_pct=Math.round(q.quote_rows/q.row_count*1000)/10;
    const c=taipeiClock();const liveReady=!!(c.session&&dates.intraday===c.today&&!intradayStale&&Number(q.quote_coverage_pct||0)>=Number(q.coverage_min_pct||80)&&q.latest_quote_time);const dayActionable=!!(liveReady&&dates.daytrade===c.today&&!daytradeStale);
    snapshot={dates,latest,errors,status,market,system,freshness:{intradayStale,daytradeStale},operational:{clock:c,quality:q,liveReady,dayActionable}};window.DOGSON_DATA_TRUTH_V1700=snapshot;window.DOGSON_INTRADAY_STALE=intradayStale;window.DOGSON_DAYTRADE_STALE=daytradeStale;window.DOGSON_INTRADAY_LIVE_READY=liveReady;window.DOGSON_DAYTRADE_ACTIONABLE=dayActionable;window.DOGSON_CLOSE_TRADE_DATE=dates.close||dates.market||'';window.DOGSON_INTRADAY_TRADE_DATE=dates.intraday||'';window.DOGSON_DAYTRADE_TRADE_DATE=dates.daytrade||'';renderTruth();try{window.dispatchEvent(new CustomEvent('dogson:data-truth',{detail:snapshot}))}catch(_){ }
  }
  function sourceLabel(){const m=currentMode(),o=snapshot?.operational;if(m==='close')return'盤後頁使用 close + market + hourly + 已完成交易日籌碼';if(m==='daytrade')return o?.dayActionable?'當沖使用已通過TWSE MIS即時品質門檻的 daytrade / intraday':'當沖即時層目前不可執行；歷史資料不顯示為現在可下單訊號';if(o?.liveReady)return'找波段使用已通過TWSE MIS品質門檻的盤中即時資料';return'找波段目前自動使用最近完整 close；5分鐘即時雷達暫停'}
  function renderTruth(){const anchor=$('#dogsonMissionV1700')||$('#dogsonViewNav');if(!anchor||!snapshot)return;let d=$('#dogsonDataTruthV1700');if(!d){d=document.createElement('details');d.id='dogsonDataTruthV1700';d.className='dogson-truth-v1700';anchor.after(d)}const {dates,latest,errors,freshness,operational}=snapshot;const lag=Object.entries(dates).filter(([,v])=>v&&latest&&v<latest);let tone='ok',badge='資料日期一致';if(errors){tone='bad';badge='部分資料讀取失敗'}else if(freshness.intradayStale||freshness.daytradeStale){tone='warn';badge='舊即時訊號已停用'}else if(!operational.liveReady){tone='warn';badge=operational.clock.session?'即時品質未達門檻':'非盤中・使用完整盤後'}else if(lag.length){tone='warn';badge='公布時差，已標示'}const items=[['市場',dates.market],['盤中結構',dates.intraday],['盤後波段',dates.close],['60分K',dates.hourly],['當沖行情',dates.daytrade],['籌碼',dates.chips]];const grid=items.map(([k,v])=>`<div class="dogson-truth-item-v1700"><div class="dogson-truth-k-v1700">${esc(k)}</div><div class="dogson-truth-v-v1700">${esc(v||'無有效交易日')}</div></div>`).join('');const q=operational.quality;const sub=latest?`最近完整交易日 ${latest}｜MIS覆蓋 ${Number(q.quote_coverage_pct||0).toFixed(1)}%｜${operational.liveReady?'即時可用':'即時未啟用'}`:'正在核對資料…';const html=`<summary><div><div class="dogson-truth-title-v1700">資料狀態・v1.7.2</div><div class="dogson-truth-sub-v1700">${esc(sub)}</div></div><span class="dogson-truth-badge-v1700 ${tone}">${esc(badge)}</span></summary><div class="dogson-truth-body-v1700"><div class="dogson-truth-grid-v1700">${grid}</div><div class="dogson-truth-note-v1700">行情日期正確不代表現在可執行。只有台北現貨盤中、行情日期為今天，且TWSE MIS個股覆蓋達 ${Number(q.coverage_min_pct||80).toFixed(0)}% 門檻時，盤中／當沖才會啟用；其他時間自動使用最近完整盤後資料或暫停當沖。</div><div class="dogson-truth-source-v1700"><b>目前頁面資料規則：</b>${esc(sourceLabel())}<br><b>即時報價：</b>TWSE MIS｜<b>市場／法人：</b>TWSE、TPEx 官方資料。</div></div>`;if(d.dataset.h!==html){d.innerHTML=html;d.dataset.h=html}}
  let timer;function refreshUI(){clearTimeout(timer);timer=setTimeout(()=>{setVersion();renderMission();renderTruth()},40)}
  function boot(){installStyle();setVersion();renderMission();loadTruth();document.addEventListener('click',e=>{if(e.target?.closest?.('.tab,#dogsonViewNav'))setTimeout(refreshUI,60)});window.addEventListener('dogson:freshness',()=>{refreshUI();setTimeout(loadTruth,60)});document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(loadTruth,50)});setInterval(()=>{if(!document.hidden)loadTruth()},60000);setTimeout(refreshUI,300);setTimeout(refreshUI,1000)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
